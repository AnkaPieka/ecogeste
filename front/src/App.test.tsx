import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  // render(<App />);
  // Commentary const linkElement = screen.getByText(/learn react/i);
  expect(true).toBeTruthy();
});
